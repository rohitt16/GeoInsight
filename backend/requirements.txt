# Core API — required
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
requests==2.32.3

# Real geospatial processing — optional.
# The API runs fine without these (it uses the synthetic fallback), but you
# need them to compute real NDVI / rainfall / surface-water stats once you
# drop actual datasets into data/. If these fail to install on your machine
# (rasterio/GDAL wheels can be picky on Windows), just comment them out —
# nothing else breaks.
geopandas==0.14.4
rasterio==1.3.10
rasterstats==0.19.0
shapely==2.0.5
numpy==1.26.4
